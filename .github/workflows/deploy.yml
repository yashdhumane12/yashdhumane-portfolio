# Deployment handled by direct push to gh-pages. This workflow is disabled.
name: Deploy (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Direct push to gh-pages is used instead"
